name: First Workflow
on:
  workflow_dispatch:

jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Printing Greeting
        run: |
          #!/bin/bash
          Add_Command=""
          console.log(`THIS IS ` + "BAKU")

